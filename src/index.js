//o index tem a função de inicializador ele vai basicamente renderizar tudo;
import React from 'react';
import ReactDOM from 'react-dom';
import App from './AppTeste';


ReactDOM.render( 
    <React.StrictMode >
    <App/>
    </React.StrictMode>,
    document.getElementById('root')
);

